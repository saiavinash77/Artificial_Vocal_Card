# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/

# Model weights (tracked conventions live in models/README.md)
models/*.onnx
models/*.pt
models/*.tflite
models/*.bin

# Captures + generated audio (udp_capture.py / CLI smoke outputs)
# Exception: the deterministic demo fixture is tracked so the README
# quickstart works on any clone. (Must ignore capture/* — a negated
# file inside an excluded directory cannot be re-included.)
capture/*
!capture/demo.hex
*.wav

# Session datasets from csv_logger.py (HUMAN SUBJECT DATA - never commit)
data/

# ESP-IDF firmware build artifacts
firmware/build/
firmware/sdkconfig.old
firmware/dependencies.lock
firmware/managed_components/
firmware/test/test_packet

# Editors / OS
.vscode/
.idea/
Thumbs.db
.DS_Store
